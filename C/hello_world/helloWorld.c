#include <stdio.h>              // 头文件: 除了核心代码外的辅助功能

int main(){                     // 函数返回值为整型; 程序(不是某个源代码文件, 而是多个源文件组成的一个程序)只能有一个主函数
    printf("hello World!\n");   // print format, 格式化输出
    return 0;                   // 返回值，代表函数的结束
}
