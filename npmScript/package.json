{
  "main": "./js/main.js",
  "version": "1.0.0",
  "name": "front-end-automaking",
  "scripts": {
    "lint": "npm-run-all --parallel lint:*",
    "lint:js": "eslint *.js",
    "lint:js:fix": "npm run lint:js -- --fix",
    "lint:css": "stylelint \"**/*.sass\"",
    "lint:json": "jsonlint --quiet *.json",
    "pretest": "npm run lint",
    "test": "mocha tests/",
    "collect": "scripty",
    "collect:serve": "scripty",
    "collect:open": "scripty",
    "watch": "npm-run-all --parallel watch:*",
    "watch:lint": "onchange -i \"**/*.js **/*.scss\" -- npm run lint",
    "watch:test": "npm t -- --watch",
    "reload": "browser-sync start --files \"**/*.html, **/*.css, **/*.js\" --server",
    "prebuild": "rm -rf dist && mkdir -p dist/images && mkdir -p dist/styles && mkdir -p dist/js",
    "build:images": "scripty",
    "build:js": "scripty",
    "build:styles": "scripty",
    "build:hash": "scripty",
    "build": "scripty",
    "precommit": "npm run lint",
    "prepush": "lint-staged"
  },
  "devDependencies": {
    "browser-sync": "^2.24.7",
    "chai": "^4.1.2",
    "cssmin": "^0.4.3",
    "eslint": "^5.5.0",
    "hashmark": "^5.0.0",
    "http-server": "^0.11.1",
    "husky": "^0.14.3",
    "imagemin-cli": "^3.0.0",
    "jsonlint": "^1.6.2",
    "lint-staged": "^7.2.2",
    "mocha": "^5.2.0",
    "npm-run-all": "^4.1.3",
    "nyc": "^13.0.1",
    "onchange": "^4.1.0",
    "opn-cli": "^3.1.0",
    "replaceinfiles": "^1.1.4",
    "scripty": "^1.7.2",
    "stylelint": "^9.5.0",
    "stylelint-config-standard": "^18.2.0",
    "uglify-es": "^3.3.9"
  },
  "nyc": {
    "exclude": [
      "**/*.spec.js",
      ".*.js"
    ]
  },
  "config": {
    "port": 3000
  },
  "lint-staged": {
    "*.js": "eslint",
    "*.less": "stylelint",
    "*.scss": "stylelint",
    "*.css": "stylelint",
    "*.json": "jsonlint --quiet"
  },
  "author": "Janemon <Janemon2331@gmail.com> (https://github.com/Janemon)",
  "description": "> In this concept, the core is **npm** and in npm the core is **pakage.json** file, and in package.json file, the core are **dependencies** and **script**. But what we do are **mainly sorounding the script**",
  "keywords": [
    "automaking"
  ],
  "license": "MIT"
}
